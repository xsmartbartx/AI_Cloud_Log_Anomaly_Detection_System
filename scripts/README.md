# Scripts

- **DB migrations:** from repo root run `npm run db:migrate` (runs `packages/db-client` Prisma migrate deploy). Or from `packages/db-client`: `npm run db:migrate` / `npm run db:migrate:dev`.
- `seed-dev.sh` – seed dev tenant/data (to be added)
- `run-local.sh` – run all services locally (to be added)
