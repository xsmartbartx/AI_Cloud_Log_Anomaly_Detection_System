# AI_Cloud_Log_Anomaly_Detection_System

Project overview — describes the system as an AI-powered predictive incident detection platform

Architecture diagram — ASCII flow showing the full pipeline from log ingestion to dashboard

Project structure — maps the full monorepo layout (apps, packages, docs, scripts)

Tech stack table — TypeScript, Node.js ≥18, pnpm 9, PostgreSQL, Prisma, Elasticsearch/OpenSearch, Kafka

Getting started — prerequisites, installation, environment variables, database setup, build & run commands

API endpoints — documented routes for Log Ingestion (POST /logs) and Model Service (POST /predict)

How it works — feature engineering (time windows, metrics), anomaly scoring, and alerting flow

Database schema — summary of all 14 PostgreSQL tables + ES index naming convention

Development roadmap — Phase 1 (MVP), Phase 2 (Advanced), Phase 3 (Enterprise)

Security — TLS, JWT/OAuth, encryption at rest, RBAC, audit logs

Scripts reference — all available pnpm commands

License — GNU GPLv3
